# normalize-ignoring-spikes
Python scirpt normalizing audio while ignoring spikes.

Run using `python normalize_ignoring_spikes.py <filename>`. More parameters via `python normalize_ignoring_spikes.py --help`.
